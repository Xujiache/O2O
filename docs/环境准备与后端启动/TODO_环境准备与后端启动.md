# 后续待办事项 (TODO) - 环境准备与后端启动

## 需要您继续确认/支持的事宜
1. **RabbitMQ 服务缺失**：
   - 当前由于缺乏 `docker` 环境支持，我已为您在本地的配置文件中移除了 `RABBITMQ_URL`，使得后端系统进入到了内存 Mock 的降级模式。
   - **操作指引**：如果您期望部署 RabbitMQ 进行开发，建议使用 Docker Desktop 并运行 `pnpm docker:dev:up`；如果已经准备了其他机器上的 RabbitMQ 服务，请将其连接字符串重新填写到 `后端/.env.development` 文件的 `RABBITMQ_URL` 字段中。

2. **前端与后续测试**：
   - 后端目前已可以在 `http://localhost:3000/docs` 访问 Swagger 接口文档，您可以随时查看后端拥有的各种 API 以及服务状态。
   - 如果需要启动某个前端（例如商户端或骑手端），可在根目录下运行对应的脚本，例如 `pnpm dev:merchant`。

3. **数据预热**：
   - 当前数据库是纯空数据库，虽然 TypeORM 已经生成了所有的空表，但是没有内置任何超级管理员账号、测试商品或角色配置。如果您需要一些测试数据，可以寻找是否有相应的 Seed 脚本，或者直接通过 API 进行初始注册创建。
