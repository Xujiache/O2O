# P7 骑手端 R1 修复提示词

> 给单 Agent 用，1 项 P3 但本轮必修；其余 15 项物理不可修，已归 P9。
> 完成后 1 个 commit + push + 在 chat 输出 "R1 修复完成，请复审"

---

```markdown
# 🔧 P7 骑手端 R1 修复任务（V2.0 单 Agent）

你是 O2O 平台骑手端 P7 的单 Agent。P7-REVIEW-01 审查仅发现 **1 项必修**，其余 15 项（R-02 + L7-01~L7-14）物理不可修，已归 P9。

## 一、必读
- `docs/P7_骑手端开发/P7_REVIEW_01.md`（本期审查报告，详述 R-01 + 为什么 R-02/L7 归 P9）
- `docs/P7_骑手端开发/P7_COMPLETION_REPORT.md`（含 14 项 L7 已知遗留）
- `骑手端/src/static/README.md`（资源占位状态）

## 二、修复清单（1 项）

### R-01 [P3 本轮必修] new-dispatch.mp3 0 字节占位补真最小有效 mp3

**问题**：与 P6/I-03 R1 修复后的 R-02 残留同款。P6 当时归 P9 没修；P7 提示词 §4.3.6 明确要求"每个资源都必须创建占位文件（哪怕 0 字节）"，但 silent.wav (45B) / marker (67B) 都是真最小有效文件，唯独 new-dispatch.mp3 是 0 字节，处理不一致。

**修法**：

1. **生成 ≤ 1KB 最小有效 mp3**（任选其一）：
   - **方案 A（推荐 ffmpeg）**：
     ```
     cd 骑手端/src/static/audio
     ffmpeg -f lavfi -i "anullsrc=r=8000:cl=mono" -t 0.1 -b:a 8k -y new-dispatch.mp3
     ```
     产出约 200 字节的 0.1 秒静音 mp3。
   - **方案 B（手写最小 ID3v2 + MP3 frame）**：如果环境无 ffmpeg，可用 Node.js 写入硬编码字节数组（约 150 字节，包含 MPEG-1 Layer 3 frame header + padding）。可参考 silent.wav 的处理思路。
   - **方案 C（无法生成时的兜底）**：保持 0 字节，但**必须**在 `骑手端/src/static/README.md` 中明确写"无 ffmpeg 环境，0 字节占位由 audioCtx.onError 兜底，归 P9-L7-08 替换真音频"，并加上等价于 silent.wav 那种"最小有效"的目标命令示例。这种方案需要在 commit message 中显式说明降级理由。

2. **更新 `骑手端/src/static/README.md`**：
   - 将 `new-dispatch.mp3` 那一行的字节数从 0 改为实际产出大小
   - 状态从"0B 占位 + onError 兜底"改为"≤1KB 真最小有效 mp3"
   - 保留 P9 真资源命令（设计师产出 ≥ 2 秒提示音 mp3 替换）

3. **不要碰**其它任何文件。

## 三、收尾要求

1. 4 项门禁全过：`pnpm lint:check && pnpm lint:stylelint:check && pnpm type-check && pnpm build:h5`
2. 在 `docs/P7_骑手端开发/P7_COMPLETION_REPORT.md` 末尾追加 `## 十四、R1 修复记录`，含：
   - R-01 修复前/修复后对比表（字节数 / 是否合法 mp3 / build 行为）
   - 选用的方案（A / B / C）+ 理由
   - 验证截图或字节数 dump
3. 1 个 commit + push：
   - subject: `fix(骑手端): P7-R1 修复 — new-dispatch.mp3 占位补最小有效（R-01）`
   - body 列出修复细节（每行 < 100 字符）
4. 在 chat 输出 "R1 修复完成，请复审"，等用户审查

## 四、绝对禁忌

- ❌ 不要碰 R-02（Sass legacy-js-api，归 P9）
- ❌ 不要碰 L7-01~L7-14（物理不可修，归 P9）
- ❌ 不要修改其它已通过模块（R1 修复范围严格限定 = 1 个 mp3 文件 + 1 个 README + 1 个完成报告追加）
- ❌ 不要重写 P7 完成报告，只追加 §十四
- ❌ commit body 行 < 100 字符
- ❌ 禁止 `--amend` 已 push 的 commit

完成后请输出 "R1 修复完成，请复审"。
```
