# P7 骑手端 R2 修复提示词

> 给单 Agent 用，1 项 P2 必修（R-03，P7-REVIEW-02 完整测试才发现的漏审项）
> 完成后 1 个 commit + push + 在 chat 输出 "R2 修复完成，请复审"

---

```markdown
# 🔧 P7 骑手端 R2 修复任务（V2.0 单 Agent）

你是 O2O 平台骑手端 P7 的单 Agent。P7-REVIEW-02 完整回归测试发现 **1 项 P2 必修（R-03）**，是前一轮 P7-REVIEW-01 漏审的真问题，不是 R1 引入的。

## 一、必读
- `docs/P7_骑手端开发/P7_REVIEW_02.md`（本期审查报告，详述 R-03 + 双向 grep 工作流改进）
- `docs/P7_骑手端开发/P7_COMPLETION_REPORT.md` §十"10/10 ✅"自报不符（需勘误）
- `骑手端/src/utils/storage.ts`（STORAGE_KEYS 当前 17 项，需追加 1 项）
- `骑手端/src/store/auth.ts`（5 处硬编码需修）

## 二、修复清单（1 项 P2，6 个位置改动）

### R-03 [P2 必修] auth.ts 5 处硬编码 `'o2o_rider_auth_extra'` → STORAGE_KEYS.AUTH_EXTRA

**Step 1：`骑手端/src/utils/storage.ts` STORAGE_KEYS 追加 1 项**

在现有 17 项之后追加（注意尾逗号 + 注释中文 + JSDoc 一致性）：

```typescript
  /** 骑手登录后辅助状态：保证金已交标记 + 健康证有效期 */
  AUTH_EXTRA: 'o2o_rider_auth_extra'
```

**Step 2：`骑手端/src/store/auth.ts` 5 处替换**

| 行 | 原 | 新 |
|---|---|---|
| 71 | `getStorage<...>('o2o_rider_auth_extra')` | `getStorage<...>(STORAGE_KEYS.AUTH_EXTRA)` |
| 99 | `setStorage('o2o_rider_auth_extra', ...)` | `setStorage(STORAGE_KEYS.AUTH_EXTRA, ...)` |
| 131 | `setStorage('o2o_rider_auth_extra', ...)` | `setStorage(STORAGE_KEYS.AUTH_EXTRA, ...)` |
| 144 | `setStorage('o2o_rider_auth_extra', ...)` | `setStorage(STORAGE_KEYS.AUTH_EXTRA, ...)` |
| 176 | `removeStorage('o2o_rider_auth_extra')` | `removeStorage(STORAGE_KEYS.AUTH_EXTRA)` |

确保 auth.ts 顶部已 import `STORAGE_KEYS`（应该已有，因为其它行已经用了 STORAGE_KEYS.TOKEN 等）。

**Step 3：grep 验证 0 残留**

```
cd 骑手端
rg -n "'o2o_rider_auth_extra'" src/
# 期望：0 命中（除了 STORAGE_KEYS 定义里那一行，那是常量本身）
```

**Step 4：勘误 `P7_COMPLETION_REPORT.md` §十**

第 277 行附近 "P5/P6 教训规避清单" 表格里的 #8（P6/I-05 STORAGE_KEYS 硬编码）那一行，把"✅"改为"✅（R2 闭环；R1 时仍残 5 处 auth_extra 硬编码，R2 全部走 STORAGE_KEYS.AUTH_EXTRA）"。

**Step 5：`P7_COMPLETION_REPORT.md` 末尾追加 §十五 R2 修复记录**

包含：
1. R-03 修复前/修复后对比表（5 个位置 + STORAGE_KEYS +1）
2. 漏审原因（前一轮只正向 grep 未反向 grep）
3. 工作流改进承诺：双向 grep 列入审查标准
4. R2 commit hash

## 三、不要碰

- ❌ 不要碰 line 179/199 的 `'o2o_rider_auth'`（pinia persist 框架 key，与 line 229-230 的 `persist.key` 配置约定一致；归 P9 可选优化）
- ❌ 不要碰 R-01 已闭环的代码（mp3 / scripts/gen-min-mp3.mjs / static/README.md）
- ❌ 不要碰其它已通过模块（R2 范围严格 = `storage.ts` +1 行 + `auth.ts` 5 处替换 + `P7_COMPLETION_REPORT.md` 勘误 + 追加 §十五）

## 四、收尾要求

1. 4 项门禁全过：`pnpm lint:check && pnpm lint:stylelint:check && pnpm type-check && pnpm build:h5`
2. 双向 grep 验证：
   - `rg -n "'o2o_rider_auth_extra'" 骑手端/src/` → 应只剩 1 处（storage.ts 常量定义）
   - `rg -n "'o2o_rider_" 骑手端/src/` → 看剩余硬编码是否都是 framework key（如 `'o2o_rider_auth'` pinia persist）
3. 1 个 commit + push：
   - subject: `fix(骑手端): P7-R2 修复 — auth_extra 5 处硬编码走 STORAGE_KEYS（R-03）`
   - body 列出每处改动（每行 < 100 字符）
4. 在 chat 输出 "R2 修复完成，请复审"

## 五、绝对禁忌

- ❌ 不要碰 R-01 / pinia persist key / 其它已通过模块
- ❌ commit body 行 < 100 字符
- ❌ 禁止 `--amend` 已 push commit
- ❌ 不要重写 P7 完成报告，只勘误 §十 + 追加 §十五

完成后请输出 "R2 修复完成，请复审"。
```
