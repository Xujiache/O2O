# ============================================================================
# Secret（占位模板，实际值通过 CI/CD 或 KMS 注入，禁止明文入仓）
# 使用方法：kubectl create secret generic backend-secret --from-env-file=.env.prod -n o2o
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: o2o
type: Opaque
stringData:
  DB_USER: "REPLACE_ME"
  DB_PASS: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  RABBITMQ_URL: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
  JWT_REFRESH_SECRET: "REPLACE_ME"
  ADMIN_SIGN_APP_KEY: "REPLACE_ME"
  ADMIN_SIGN_APP_SECRET: "REPLACE_ME"
  MINIO_ACCESS_KEY: "REPLACE_ME"
  MINIO_SECRET_KEY: "REPLACE_ME"
  AMAP_WEB_KEY: "REPLACE_ME"
  AMAP_SERVER_KEY: "REPLACE_ME"
  CRYPTO_AES_KEY: "REPLACE_ME"
  CRYPTO_HMAC_KEY: "REPLACE_ME"
  TIMESCALE_USER: "REPLACE_ME"
  TIMESCALE_PASS: "REPLACE_ME"
