import { Module } from '@nestjs/common'

/**
 * 消息推送模块（占位）
 * 功能：对齐 PRD §3.5.1 消息推送服务，统一 APP 推送、微信订阅消息、短信、站内信分发
 * 参数：无
 * 返回值：MessageModule
 * 用途：P3 阶段扩展；P1 仅占位
 */
@Module({})
export class MessageModule {}
