import { Module } from '@nestjs/common'

/**
 * 支付模块（占位）
 * 功能：对齐 PRD §3.5.2 支付服务，对接微信/支付宝，支持下单、退款、对账、分账
 * 参数：无
 * 返回值：PaymentModule
 * 用途：P4 阶段扩展；P1 仅占位
 */
@Module({})
export class PaymentModule {}
