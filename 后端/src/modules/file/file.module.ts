import { Module } from '@nestjs/common'

/**
 * 文件存储模块（占位）
 * 功能：对齐 PRD §3.5.1 文件存储服务，对接 MinIO/OSS，支持图片、视频、凭证上传管理
 * 参数：无
 * 返回值：FileModule
 * 用途：P3 阶段扩展；P1 仅占位
 */
@Module({})
export class FileModule {}
