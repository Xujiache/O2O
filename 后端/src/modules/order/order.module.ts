import { Module } from '@nestjs/common'

/**
 * 订单模块（占位）
 * 功能：对齐 PRD §3.5.2 订单服务，管理外卖/跑腿订单创建、状态流转、库存扣减、取消/退款
 * 参数：无
 * 返回值：OrderModule
 * 用途：P4 阶段扩展；P1 仅占位
 */
@Module({})
export class OrderModule {}
