import { Module } from '@nestjs/common'

/**
 * 财务结算模块（占位）
 * 功能：对齐 PRD §3.5.2 财务结算服务，执行分账规则、账单生成、提现、平台营收统计
 * 参数：无
 * 返回值：FinanceModule
 * 用途：P4 阶段扩展；P1 仅占位
 */
@Module({})
export class FinanceModule {}
