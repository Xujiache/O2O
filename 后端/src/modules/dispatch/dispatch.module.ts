import { Module } from '@nestjs/common'

/**
 * 配送调度模块（占位）
 * 功能：对齐 PRD §3.5.2 配送调度服务，实现智能派单、订单分发、顺路单匹配、骑手运力调度
 * 参数：无
 * 返回值：DispatchModule
 * 用途：P4 阶段扩展；P1 仅占位
 */
@Module({})
export class DispatchModule {}
